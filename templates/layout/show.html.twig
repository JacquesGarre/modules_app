{% extends 'base.html.twig' %}

{% block title %}Administration | Layout {{ layout.title }} | {{ parent() }}{% endblock %}

{% block body %}

    <a class="btn btn-sm btn-primary mb-5"
        href="{{ path('app_administration') }}">
        <i class="fa-solid fa-chevron-left"></i> Back
    </a>
    <h3>Layout {{ layout.title }}</h3>    
    {% include 'layout/_layout_edit.html.twig' %}
{% endblock %}