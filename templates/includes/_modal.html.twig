<!-- Modal -->
<div class="modal fade"
    id="modal"
    tabindex="-1"
    data-controller="modal"
    data-modal-target="modal"
    aria-labelledby="modalLabel"
    aria-hidden="true"
    data-action="form:success->modal#close"
>
  <div class="modal-dialog">
  </div>
</div>
