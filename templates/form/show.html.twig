{% extends 'base.html.twig' %}

{% block title %}Administration | Form {{ form.title }} | {{ parent() }}{% endblock %}

{% block body %}
    
    <a class="btn btn-sm btn-primary mb-5"
        href="{{ path('app_administration') }}">
        <i class="fa-solid fa-chevron-left"></i> Back
    </a>
    <h3>{{ form.title }}</h3>    
    <div class="row">
        {% include 'form/form.html.twig' %}
    </div>

{% endblock %}