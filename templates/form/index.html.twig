<h3>
    Forms
    <button type="button" class="btn btn-primary" data-controller="modal" data-action="modal#open" data-modal-url-value="{{ path('app_form_add', {'moduleId': module.id }) }}">
        New form
    </button>
</h3>    
<table 
    id="form-table" 
    class="table table-striped"
    data-controller="table"
    data-url="{{ path('app_form_index', {'moduleId': module.id }) }}"
    data-action="modal:success->table#reload"
    data-table="form"
>
    {% include 'form/_table.html.twig' %}
</table>

